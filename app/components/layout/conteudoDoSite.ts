/**
 * Envoltório de tudo que não é sobreposição: cabeçalho, página e rodapé.
 *
 * O modal da ficha desfoca este elemento enquanto está aberto — o desfoque mora
 * no conteúdo, não no `backdrop-filter` do overlay. Ver `ModalDaEspecie`.
 */
export const ID_CONTEUDO_DO_SITE = "conteudo-do-site";
